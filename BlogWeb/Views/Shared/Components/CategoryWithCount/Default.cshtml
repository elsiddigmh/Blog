@model IEnumerable<BlogWeb.Models.VM.CategoryWithCountVM>

@foreach(var category in Model)
{
    <li>
        <a asp-controller="Category" asp-action="Show" asp-route-id="@category.Id">@category.Name<span class="ml-auto"> (@category.PostCount)</span></a>
    </li>
}